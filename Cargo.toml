[package]
name = "rir_generator"
version = "0.1.0"
edition = "2021"

[lib]
name = "rir_generator"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.18.3", features = ["extension-module"] }
numpy = "0.18"
